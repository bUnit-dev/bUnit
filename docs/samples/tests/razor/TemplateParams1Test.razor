@inherits TestContext

@code
{
#if NET6_0
  [Fact]
  public void Test()
  {
    var cut = Render(@<TemplateParams Items=@(new[] { "Foo", "Bar", "Baz" }) TItem="string">
                        <Template>
                          <span>@context</span>
                        </Template>
                      </TemplateParams>);
  }
#endif
}